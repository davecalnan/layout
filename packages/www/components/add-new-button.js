const AddNewButton = ({ onClick }) => (
  <button
    className="w-full h-16 flex justify-center items-center text-left bg-gray-200 rounded shadow-inner px-4 mb-2"
    onClick={onClick}
  >
    +
  </button>
)

export default AddNewButton
